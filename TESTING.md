# Тестирование проекта dinIslam

## 📋 Структура тестов

Проект включает базовые unit тесты для ключевых компонентов:

```
dinIslamTests/
├── dinIslamTests.swift          # Базовые тесты проекта
├── QuizUseCaseTests.swift       # Тесты бизнес-логики викторины
└── StatsManagerTests.swift      # Тесты управления статистикой
```

## 🧪 Что тестируется

### QuizUseCaseTests
- ✅ Загрузка вопросов (20 вопросов за сессию)
- ✅ Обработка случаев с меньшим количеством вопросов
- ✅ Уникальность вопросов в сессии
- ✅ Перемешивание ответов
- ✅ Сохранение данных вопроса при перемешивании
- ✅ Расчет результатов викторины
- ✅ Обработка ошибок

### StatsManagerTests
- ✅ Обновление статистики
- ✅ Отслеживание неправильных вопросов
- ✅ Удаление неправильных вопросов
- ✅ Очистка неправильных вопросов
- ✅ Получение списка неправильных вопросов
- ✅ Сброс статистики
- ✅ Расчет точности ответов

### dinIslamTests
- ✅ Проверка структуры проекта
- ✅ Сериализация моделей данных
- ✅ Логика определения новых рекордов

## 🚀 Запуск тестов

### В Xcode
1. Откройте проект в Xcode
2. Нажмите `Cmd + U` или выберите **Product → Test**
3. Все тесты должны пройти успешно

### Через командную строку
```bash
xcodebuild test -scheme dinIslam -destination 'platform=iOS Simulator,name=iPhone 15 Pro'
```

## 📊 Покрытие тестами

Текущее покрытие:
- **QuizUseCase**: ~80% основных методов
- **StatsManager**: ~90% основных методов
- **Models**: Базовое покрытие

## 🔧 Добавление новых тестов

1. Создайте новый файл в `dinIslamTests/`
2. Наследуйтесь от `XCTestCase`
3. Используйте моки для зависимостей:

```swift
class MockQuestionsRepository: QuestionsRepositoryProtocol {
    var mockQuestions: [Question] = []
    var shouldFail = false
    
    func loadQuestions(language: String) async throws -> [Question] {
        if shouldFail {
            throw QuestionsError.fileNotFound
        }
        return mockQuestions
    }
}
```

## ✅ Проверка перед коммитом

Перед отправкой кода убедитесь:
- [ ] Все тесты проходят (`Cmd + U`)
- [ ] Нет warning в тестах
- [ ] Новые функции покрыты тестами (если возможно)

---

**Примечание**: Это базовая структура тестов. По мере развития проекта рекомендуется расширять покрытие тестами.

